import type {VercelRequest,VercelResponse} from '@vercel/node';
import {readDB,writeDB,currentUser} from './_github';
const defaults={apkUrl:'https://github.com/mythosnetworkbr-cell/Myth-s-Network-Site/releases/latest',discordUrl:'https://discord.com/app',storeUrl:'https://discord.com/app',supportUrl:'/suporte',videoUrl:'https://www.youtube.com/embed/Ny780FeCJnA?rel=0&modestbranding=1',logoUrl:'/assets/icon.png',iconUrl:'/assets/icon.png'};
export default async function handler(req:VercelRequest,res:VercelResponse){try{const {db,sha}=await readDB();const settings={...defaults,...((db as any).settings||{})};if(req.method==='GET')return res.status(200).json({settings});const user=currentUser(req,db);if(!user||user.role!=='admin')return res.status(403).json({error:'Acesso restrito ao administrador.'});if(req.method!=='POST')return res.status(405).json({error:'Método não permitido'});const b:any=req.body||{};const next={...settings};for(const key of Object.keys(defaults)){if(b[key]!==undefined){const value=String(b[key]).trim();if(value.length>2000)return res.status(400).json({error:`Campo ${key} muito grande.`});if(value&&key!=='supportUrl'){try{const u=new URL(value,req.headers.host?`https://${req.headers.host}`:'https://mythos.local');if(!['http:','https:'].includes(u.protocol))throw 0}catch{return res.status(400).json({error:`O campo ${key} precisa ser uma URL válida.`})}}next[key]=value;}}(db as any).settings=next;await writeDB(db,sha,'admin: atualizar configurações do site');return res.status(200).json({settings:next});}catch(e:any){return res.status(500).json({error:e.message||'Erro interno'});}}