// Conteúdo canônico extraído dos componentes originais do mythos-source.zip.
// Cada categoria preserva os textos, punições, capítulos, tabelas e listas do material original.
export type RuleSection={number:string;title:string;items:string[]};
export const MYTHOS_RULES = 