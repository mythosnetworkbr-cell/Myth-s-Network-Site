{
  "name": "mythos-network-site",
  "version": "1.0.0",
  "private": true,
  "description": "Rede Mythøs — site oficial e suporte",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node full-build.js && node police-build.js && node cargos-build.js && node ponto-build.js && node desktop-build.js && node apk-version-build.js && node analytics-build.js && node history-build.js && node rejeitados-build.js"
  },
  "dependencies": {
    "@vercel/blob": "latest",
    "@vercel/node": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  },
  "engines": { "node": ">=20" }
}
