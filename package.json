{
  "name": "lumys-books",
  "version": "2.0.1",
  "private": true,
  "description": "LÜMYS — plataforma de livros, leitura digital e publicação",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prebuild": "node --check lumys-build.js && node --check lumys-hotfix.js",
    "build": "node lumys-build.js && node lumys-hotfix.js",
    "verify": "node --check lumys-build.js && node --check lumys-hotfix.js"
  },
  "dependencies": {
    "@vercel/blob": "latest",
    "@vercel/node": "latest"
  },
  "devDependencies": {
    "@types/node": "latest"
  },
  "engines": { "node": ">=20" }
}
